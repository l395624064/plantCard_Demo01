# card module note

This module currently contains domain types/rules plus expandable model/view scaffolding.

- Runtime core used by main flow: `CardTypes`, `CardGeometry`, `CardRuleUtils`.
- Files under `model/*` and `view/*` are scaffolding for future expansion.
- Do not assume all scaffolding files are wired into the current main gameplay loop.
